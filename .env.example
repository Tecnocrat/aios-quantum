# ============================================================
# AIOS Quantum - Environment Configuration
# ============================================================
# 
# SECURITY PROTOCOL: AINLP.security[MEMBRANE-BOUNDARY]
# 
# 1. Copy this file to .env: cp .env.example .env
# 2. Fill in your actual API keys
# 3. NEVER commit .env to git (it's in .gitignore)
# 4. See docs/SECRETS_PROTECTION.md for security guidelines
#
# ============================================================

# ===== IBM Cloud Account =====
# Your IBM Cloud account ID (for reference only, not a secret)
IBM_CLOUD_ACCOUNT_ID=your_account_id_here

# ===== IBM Quantum Platform =====
# Get token from: https://quantum.ibm.com/ → Account → API Token
# Recommended: Enable "Regenerate on leak" in IBM Cloud settings
IBM_QUANTUM_TOKEN=your_quantum_token_here

# IBM Quantum Instance (CRN format)
# Get with: ibmcloud resource service-instance open-instance --output json
IBM_QUANTUM_INSTANCE=crn:v1:bluemix:public:quantum-computing:us-east:a/<account_id>:<instance_id>::

# Channel: ibm_cloud for Qiskit Runtime (recommended)
IBM_QUANTUM_CHANNEL=ibm_cloud

# ===== IBM Cloud Object Storage =====
# Service: aios-quantum-storage (Lite plan - 25GB free)
# Get credentials: IBM Cloud Console → COS → Service credentials
#
# IMPORTANT: Use SERVICE credentials, not user API keys!
# Create via: "New credential" → Role: Manager → HMAC: false
COS_API_KEY=your_cos_service_credential_apikey
COS_INSTANCE_ID=crn:v1:bluemix:public:cloud-object-storage:global:a/<account_id>:<instance_id>::
COS_ENDPOINT=https://s3.us-east.cloud-object-storage.appdomain.cloud

# COS Buckets (created in us-east region)
COS_BUCKET_EXPERIMENTS=aios-quantum-experiments
COS_BUCKET_RESULTS=aios-quantum-results
COS_BUCKET_NAME=aios-quantum-experiments

# ===== IBM Cloudant NoSQL Database =====
# Service: aios-quantum-metadata (Lite plan - 1GB free)
# Get credentials: IBM Cloud Console → Cloudant → Service credentials
CLOUDANT_API_KEY=your_cloudant_service_credential_apikey
CLOUDANT_HOST=<instance_id>-bluemix.cloudantnosqldb.appdomain.cloud
CLOUDANT_URL=https://<instance_id>-bluemix.cloudantnosqldb.appdomain.cloud
CLOUDANT_DATABASE=quantum_topology

# ===== IBM Cloud CLI Reference =====
# Install: https://cloud.ibm.com/docs/cli
# Path: C:\Program Files\IBM\Cloud\bin\ibmcloud.exe
# Login: ibmcloud login -a https://cloud.ibm.com -u passcode -p <PASSCODE>
# Get passcode: https://identity-3.us-south.iam.cloud.ibm.com/identity/passcode

# ===== API Key Best Practices =====
# 1. Use SERVICE ID keys (not user API keys) for programmatic access
# 2. Enable "Disable on leak" for all service credentials
# 3. Rotate keys every 90 days
# 4. Use separate keys for dev/prod environments
# 5. Monitor auth logs in IBM Cloud Console

# ===== Local Development =====
LOG_LEVEL=INFO
EXPERIMENT_LOCAL_PATH=./experiments
